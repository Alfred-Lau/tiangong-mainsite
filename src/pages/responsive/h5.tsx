import { FC } from 'react';
import styles from './h5.less';

const H5Responsive: FC = (props) => {
  return <div className={styles.H5Responsive}>H5Responsive fix header</div>;
};

export default H5Responsive;
